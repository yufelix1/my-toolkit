version: '3.8'

services:
  toolkit:
    build: .
    container_name: my-toolkit
    ports:
      - "5000:5000"
    volumes:
      - ./:/app
      # 挂载你需要处理的文件目录（可自行添加多个）
      - /path/to/your/data:/data:ro   # 示例：只读挂载
    restart: unless-stopped